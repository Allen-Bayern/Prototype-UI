<style is:global>
  .base-dialog-demo-mask,
  .base-dialog-demo-content {
    transition: opacity 0.2s ease;
  }

  .base-dialog-demo-content {
    transition-property: opacity, scale;
  }

  .base-dialog-demo-mask[data-transition-state='closed'],
  .base-dialog-demo-mask[data-transition-state='leaving'],
  .base-dialog-demo-content[data-transition-state='closed'],
  .base-dialog-demo-content[data-transition-state='leaving'] {
    opacity: 0;
    pointer-events: none;
  }

  .base-dialog-demo-content[data-transition-state='closed'],
  .base-dialog-demo-content[data-transition-state='leaving'] {
    scale: 0.96;
  }

  .base-dialog-demo-mask[data-transition-state='entering'],
  .base-dialog-demo-mask[data-transition-state='entered'],
  .base-dialog-demo-content[data-transition-state='entering'],
  .base-dialog-demo-content[data-transition-state='entered'] {
    opacity: 1;
  }

  .base-dialog-demo-content[data-transition-state='entering'],
  .base-dialog-demo-content[data-transition-state='entered'] {
    scale: 1;
  }
</style>
