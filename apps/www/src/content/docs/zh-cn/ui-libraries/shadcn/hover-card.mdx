---
title: 'Hover Card'
description: '基于 Proto UI 重构的 Shadcn Hover Card。'
---

import PrototypePreviewer from '@/components/PrototypePreviewer/PrototypePreviewer.astro';

当前页面展示 Proto UI 中 `shadcn` 风格的 `hover-card` 条目。它兼容官方 Root 的 controlled/uncontrolled、`openDelay`、`closeDelay`，以及 Content 的 `side`/`align` 子集，并复刻官方 `w-64`、popover color、border、shadow 和方向感知动画。受 Base 能力边界影响，portal、碰撞检测、Arrow、forceMount 和数值 offset 尚未兼容。

<PrototypePreviewer demoId="demo-shadcn-hover-card" initialRuntime="wc" />
