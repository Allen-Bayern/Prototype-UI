---
title: Separator
sidebar:
  order: 7
---

import { PrototypePreviewer } from '@/components/PrototypePreviewer';
import BrutalistPageStyle from '@/components/BrutalistPageStyle.astro';

<BrutalistPageStyle />
> 状态：已随公开 `0.2.0-rc.7` package 发布，可通过 npm `next` 获取，并以相同 surface 进入 draft stable `0.2.0` release train。

Brutalist Separator 保留 Base orientation 与 decorative accessibility 语义，并渲染为平涂 `2px` ink 分隔线。

用于消息日期分组、Card 分区、导航列表和表单区域。Separator 不拥有 spacing 策略；周围组合负责间距。

<div class="brutalist-demo-frame">
  <PrototypePreviewer demoId="demo-brutalist-separator" initialRuntime="wc" />
</div>
