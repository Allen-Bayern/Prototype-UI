---
title: 'Rule'
desp: 'Rule 规范'
description: 'Rule 规范'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-RULE-0001',
    title: 'Rule 不是信息通路',
    summary:
      'Rule 表达条件到意图的声明式关系，不承担 Props、Feedback、Event、Expose 或 Context 的信息传递身份。',
    quote:
      'Rule is not an information channel. It expresses the declarative relationship "when observable conditions hold, produce clear intent."',
    criteria: [
      'Rule 必须被识别为声明式条件到意图的语法，而不是信息通路。',
      'Rule 的输入来自其他模块或通路暴露的可观察值。',
      'Rule 的意图交由对应模块或执行边界实现。',
    ],
    tests: '作为身份契约，被 when、intent 与 matrix 测试共同承接。',
    source: 'spec/contracts/C-RULE-0001.yaml',
  },
  {
    id: 'C-RULE-0002',
    title: 'Rule 是可选但推荐的语法',
    summary: 'Rule 不提高原型语法表达力上限，但当逻辑能被清晰表达为条件到意图时，它是推荐路径。',
    quote:
      'When a behavior can be expressed clearly by Rule, Rule is the official recommended path because it preserves prototype-author intent as an analyzable structure.',
    criteria: [
      'Rule 不得被描述为其他原型语法表达能力的必要前提。',
      '能清晰表达为“条件成立则产生意图”时，官方应推荐使用 Rule。',
      '历史依赖、宿主特化或任意副作用逻辑不应被强行塞入 Rule。',
    ],
    tests: '作为指导性契约，通过文档、原型实践和后续 rule matrix 覆盖体现。',
    source: 'spec/contracts/C-RULE-0002.yaml',
  },
  {
    id: 'C-RULE-0003',
    title: 'RuleIR 必须可序列化',
    summary: 'Rule 的中间产物不得携带函数、宿主对象、runtime closure 或 live handle。',
    quote:
      "Rule's intermediate product must be fully serializable RuleIR, which in practice should be reducible to a string-serializable structure.",
    criteria: [
      'RuleIR 必须由可序列化数据组成。',
      'RuleIR 不得包含函数、宿主对象、runtime closure 或 adapter 私有对象。',
      '需要 execution handle 时，应通过 RuleIR 外部 side table 关联。',
    ],
    tests: 'T-RULE-0001 规划 serializable RuleIR 边界测试。',
    source: 'spec/contracts/C-RULE-0003.yaml',
  },
  {
    id: 'C-RULE-0004',
    title: 'def.rule 是 setup-only 声明 API',
    summary: '`def.rule(spec)` 只能在 setup 期声明 Rule，runtime 不能新增 Rule。',
    quote:
      '`def.rule(spec)` is a setup-only declaration API. Prototype authors may declare Rules only during setup; runtime code must not add new Rules.',
    criteria: [
      '`def.rule(spec)` 必须只能在 setup 阶段调用。',
      'runtime 阶段不得通过 `def.rule` 或等价入口新增 Rule。',
      '`when` 与 `intent` 在 setup 期只记录可分析数据，不执行 runtime 逻辑。',
    ],
    tests: 'T-RULE-0001 规划 setup-only declaration 与 RuleSpec recording 测试。',
    source: 'spec/contracts/C-RULE-0004.yaml',
  },
  {
    id: 'C-RULE-WHEN-0001',
    title: 'Rule when 表达式是纯条件',
    summary:
      '`when` 读取已声明的可观察输入并产出 boolean，不写 state、feedback、context，也不匹配瞬时 event。',
    quote:
      'Rule `when` is a pure condition expression. It reads declared observable inputs and produces a boolean result.',
    criteria: [
      '`when` 求值必须无副作用。',
      'v0 核心表达式包含 `true`、`false`、`eq`、`not`、`all` 与 `any`。',
      '`eq` 使用严格相等，不做隐式转换、深比较或自定义 comparator。',
      'Rule 不直接匹配瞬时 event occurrence。',
    ],
    tests: 'T-RULE-WHEN-0001 覆盖纯条件、表达式集合、严格相等和 event 边界。',
    source: 'spec/contracts/C-RULE-WHEN-0001.yaml',
  },
  {
    id: 'C-RULE-WHEN-0002',
    title: 'Rule when 输入由模块提供',
    summary:
      'Rule core 不创造输入源；props、state、context 或其他输入必须由对应模块或扩展定义 identity、读取与重评估语义。',
    quote:
      'Observable props, state, context, or other inputs in `when` must be provided by the corresponding module or a Rule extension module.',
    criteria: [
      '每个 `when` 输入 surface 必须定义 RuleIR 中的可序列化依赖 identity。',
      '每个输入 surface 必须定义 runtime 如何读取当前值。',
      '每个输入 surface 必须定义依赖变化如何触发 Rule 重评估。',
      'host/environment `meta` 输入暂不作为稳定核心命名。',
    ],
    tests: 'T-RULE-WHEN-0002 与 matrix fixture 覆盖 props/state/context 依赖维度。',
    source: 'spec/contracts/C-RULE-WHEN-0002.yaml',
  },
  {
    id: 'C-RULE-INTENT-0001',
    title: 'Rule intent 记录可分析操作',
    summary: '`IntentBuilder` 只记录明确操作，不在 setup 期执行副作用、调用 callback 或触达 host。',
    quote:
      'Rule `intent` only records clear, analyzable operations. During setup, `IntentBuilder` must not directly execute side effects.',
    criteria: [
      '`IntentBuilder` 必须记录操作，而不是在 setup 期执行操作。',
      'Rule intent 不得包含任意 callback 或宿主特定命令式逻辑。',
      '同一 Rule 内记录的 intent op 顺序必须保留。',
      '不同 intent channel 的合并与执行语义由各自契约定义。',
    ],
    tests: 'T-RULE-INTENT-0001 规划 intent operation recording 测试。',
    source: 'spec/contracts/C-RULE-INTENT-0001.yaml',
  },
  {
    id: 'C-RULE-INTENT-FEEDBACK-STYLE-0001',
    title: 'feedback.style 是稳定的 v0 Rule intent',
    summary:
      '`i.feedback.style.use(...)` 记录视觉反馈样式意图，并复用 feedback.style 的 token 与 merge 语义。',
    quote: '`i.feedback.style.use(...handles)` is the stable v0 Rule intent channel.',
    criteria: [
      '`i.feedback.style.use(...handles)` 是 v0 稳定 Rule intent channel。',
      '输入必须遵守 `feedback.style` 的 handle 与 token 约束。',
      'active Rule 的 style token 按 Rule 声明顺序收集。',
      '收集后的 token 经 feedback semantic merge，Rule 失效时移除自身贡献。',
    ],
    tests: 'T-RULE-INTENT-FEEDBACK-STYLE-0001 和 T-RULE-MATRIX-0001 覆盖稳定 style intent。',
    source: 'spec/contracts/C-RULE-INTENT-FEEDBACK-STYLE-0001.yaml',
  },
  {
    id: 'C-RULE-RUNTIME-0001',
    title: 'Rule runtime 产出 Plan 或等价优化执行',
    summary:
      'runtime 根据当前输入评估 RuleIR，收集 active intent，并产出 semantic Plan；扩展可以在语义等价时优化。',
    quote:
      'Runtime must evaluate RuleIR against current observable inputs, select active Rules, collect intent in declaration order, and produce a semantic Plan.',
    criteria: [
      'Rule runtime 根据当前输入评估 `when`。',
      'active Rule 的 intent 按声明顺序收集。',
      'v0 默认 Plan 是 `style.tokens`，不是宿主产物。',
      'adapter 或扩展优化路径必须与默认 Plan 语义等价。',
    ],
    tests: 'T-RULE-MATRIX-0001 规划 stable matrix 的 runtime behavior 覆盖。',
    source: 'spec/contracts/C-RULE-RUNTIME-0001.yaml',
  },
  {
    id: 'C-RULE-EXTENSION-0001',
    title: 'Rule 扩展可添加二级 when、intent 或执行路径',
    summary:
      '新增 when 输入、intent channel 或优化执行路径可以作为 rule 补丁模块存在，但不能削弱 core 语义。',
    quote:
      'New `when` inputs, `intent` channels, or optimized execution paths may normally be packaged as patch modules around `module-rule`.',
    criteria: [
      '新增 `when` 输入可以由 rule 扩展模块提供。',
      '新增 `intent` channel 必须定义自身语义。',
      '扩展模块可以 short-circuit 默认 Plan，但必须承担等价执行或委托责任。',
      'Web selector 优化不得作为 Rule core 的设计约束。',
    ],
    tests: '当前作为二级扩展契约记录，后续由模块与 adapter 契约测试承接。',
    source: 'spec/contracts/C-RULE-EXTENSION-0001.yaml',
  },
];

## Rule 是什么？

`Rule` 是 Proto UI 中表达“当某些条件成立时，产生某种明确意图”的声明式语法。它不是信息通路，也不直接在 Component、User、App Maker 或其他 Component 之间传递信息。

Rule 的价值在于把原型作者的意图保留为可分析结构。命令式 callback 可以表达很多同类逻辑，但 adapter 很难稳定理解那段代码想做什么；Rule 则把条件和意图拆开，使 adapter 有机会在不依赖 compiler 级静态分析的情况下做优化。

Rule 是可选语法。砍掉 Rule 不应让原型失去表达同类行为的能力；但当逻辑本来就能清晰写成“条件成立则产生意图”时，Rule 是官方推荐路径。

## 执行时期

<SpecPhaseBadge label="setup-only" />

Rule 的声明发生在 setup 期：

```ts
def.rule(({ when, intent }) => ({
  when: when.eq(when.props('disabled'), true),
  intent: intent.feedback.style.use(tw('opacity-50')),
}));
```

`def.rule(spec)` 只记录 `when` 与 `intent`。它不会在 setup 期读取当前 runtime props，不会写 state，不会写 feedback，也不会触达 host。

setup-only API 返回的移除或撤销函数默认也只能在 setup 期使用。Rule declaration 的移除能力如果存在，也不能被闭包保存到 runtime 里作为 lifecycle disposer 或 escape hatch。

<SpecPhaseBadge label="runtime" />

runtime 期会根据当前可观察输入评估 RuleIR，选出 active Rule，按声明顺序收集 intent，并产出 semantic Plan。v0 稳定默认 Plan 是 `style.tokens`。

默认 runtime 执行路径不直接写 DOM class、attribute、native view 或其他宿主对象。adapter 或扩展模块可以 short-circuit Plan，生成更适合宿主的优化产物，但优化产物不能反过来定义 Rule core 语义。

## when

`when` 是纯条件表达式。它读取其他模块提供的可观察输入，并产出 boolean。

v0 核心表达式包括：

```txt
true
false
eq(a, b)
not(expr)
all([...exprs])
any([...exprs])
```

`eq` 使用严格相等，不做隐式类型转换、深比较或自定义 comparator。`all([])` 是 `true`，`any([])` 是 `false`。

Rule 不直接匹配瞬时 event occurrence。事件驱动的 Rule 需要先把事件维度表达为可观察状态，例如 hover、pressed、focused 这类 interaction state，然后由 Rule 读取状态。

## when 输入来源

Rule core 不创造输入源。`when` 中的 props、state、context 或其他输入，都必须由对应模块或 rule 扩展提供。

稳定的核心覆盖包括：

| 输入来源  | 当前语义                                                       |
| --------- | -------------------------------------------------------------- |
| `props`   | 读取 resolved props，依赖 prop identity，prop 变化可触发重评估 |
| `state`   | 读取 state view 当前值，Owned / Borrowed / Observed 都可被观察 |
| `context` | 读取整个 context value；不存在 provider 时为 `null`            |

context path access 仍是实现债。当前核心契约只固化 whole-value context dependency，不把 `w.ctx(key).path(...)` 当作稳定 v0 语义。

host/environment 输入目前也不归入稳定核心命名。现有 `meta` 相关实践属于二级或待治理抽象，未来可能被更系统的环境配置模型替代。

## intent

`intent` 记录可分析操作，而不是在 setup 期执行操作。它不能携带任意 callback，也不能把宿主命令式逻辑塞进 RuleIR。

不同 intent channel 的合并与执行语义由各自契约定义。Rule core 不提供隐式跨通道优先级。

## feedback.style intent

v0 稳定的 Rule intent 是 `feedback.style`：

```ts
def.rule(({ when, intent }) => ({
  when: when.eq(when.state(pressed), true),
  intent: intent.feedback.style.use(tw('translate-y-px opacity-80')),
}));
```

`i.feedback.style.use(...handles)` 记录视觉反馈样式意图。它不扩展 `feedback.style` token 语言，输入仍必须遵守 feedback.style 的 handle 与 token 约束。

active Rule 的 style token 按 Rule 声明顺序收集，然后交给 feedback semantic merge。Rule 失效时，它贡献的 style token 必须在下一次评估中移除。

这条链路可以理解为：

```txt
observable inputs
  -> when evaluation
  -> active rule intents
  -> style.tokens plan
  -> feedback semantic merge
  -> feedback runtime style layer
  -> translation layer
```

Rule 产出的是可翻译 style 语义结果，不是 DOM class 或 CSS selector。Web selector 优化是 Rule 思路的一个下游成果，但不是 Rule core 的设计约束。

## Deferred Scope

以下内容已经记录为断口或实现债，但不属于当前稳定 v0 Rule core：

| 项目 | 状态 |
| --- | --- |
| `intent.state` | 计划能力；layer stack、rollback、baseline、reason 等语义尚未实现为 v0 保证 |
| context path access | 当前只稳定 whole-value context dependency |
| `meta` 命名 | host/environment 输入的抽象层级和命名仍待治理 |
| RuleIR live handle side table | 需要继续收敛实现，避免 RuleIR 携带 live handle |
| setup-time removal handle | 需要确保 setup-only removal 不变成 runtime disposer |

这些项目可以被 fixture 或 planned test 看见，但不得被视为当前 required matrix 覆盖。

## 契约实体预览

<SpecContractPreview items={contractPreviews} locale="zh-cn" />

## 与测试的关系

Rule 的测试实体按“维度测试 + 矩阵测试”组织，避免组合爆炸：

| 测试实体 | 主要覆盖 |
| --- | --- |
| `T-RULE-0001` | `def.rule` setup-only、RuleSpec recording、RuleIR serialization、setup-only removal |
| `T-RULE-WHEN-0001` | when 纯表达式、严格相等、逻辑表达式、event 边界 |
| `T-RULE-WHEN-0002` | props/state/context 依赖维度 |
| `T-RULE-INTENT-0001` | intent operation recording 与 channel independence |
| `T-RULE-INTENT-FEEDBACK-STYLE-0001` | 稳定的 feedback.style intent |
| `T-RULE-MATRIX-0001` | `props/state/context × feedback.style` stable matrix |

`intent.state` 的组合可以出现在 matrix fixture 中，但它是 deferred cell，不是当前 required coverage。

## 与其他规范的关系

- `Core` 定义 setup/runtime 阶段边界；Rule 声明遵守 setup-only，Rule evaluation 发生在 runtime。
- `Props`、`State`、`Context` 可以为 `when` 提供可观察输入，但 Rule 不拥有这些信息来源。
- `Event` 不被 Rule 直接匹配；事件维度应先进入可观察状态。
- `Feedback` 定义 `feedback.style` 的 token 与 semantic merge；Rule 的稳定 v0 intent 复用这套语义。
- `Lifecycle` 管理实例存活与 runtime callback 边界；Rule 的 setup-time removal 不能变成 lifecycle disposer。
- `Adapter` 与 rule extension 可以做优化，但必须保持默认 Plan 的语义等价。
