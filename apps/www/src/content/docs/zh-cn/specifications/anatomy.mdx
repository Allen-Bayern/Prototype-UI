---
title: 'Anatomy'
desp: 'Anatomy 规范'
description: 'Anatomy 规范'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-ANATOMY-0001',
    title: 'Anatomy 是结构语义，不是信息通路',
    summary:
      'Anatomy 描述复合原型 family 内部的 role、relation、约束与受限观察；它不是组件间通用通信机制。',
    quote: 'Anatomy is not a Proto UI information channel.',
    criteria: [
      'Anatomy 必须被识别为结构语义系统，而不是新增信息通路。',
      'Anatomy 不创建、移动、补全 part，也不改写 template 或宿主树。',
      '通用组件间通信仍属于 Context。',
    ],
    tests:
      'Anatomy 的定位主要由契约文本约束；runtime 行为通过 T-ANATOMY-0002 与 T-ANATOMY-ORDER-0001 间接覆盖。',
    source: 'spec/contracts/C-ANATOMY-0001.yaml',
  },
  {
    id: 'C-ANATOMY-0002',
    title: 'Family spec 是 static-only 定义',
    summary:
      '稳定 anatomy family 由 reference identity 的 family token 标识，并可在 token 上携带 canonical spec。',
    quote:
      'A stable anatomy family is defined by a reference-identity family token that may carry its canonical spec.',
    criteria: [
      '`createAnatomyFamily(debugName, spec)` 是 stable family spec 的 static-only API。',
      'family identity 由 token 引用决定，不由 `debugName` 决定。',
      '稳定库级 family 不应要求每个 part 在 setup 中重复注册同一份 spec。',
    ],
    tests: 'T-ANATOMY-0001 覆盖 embedded family spec 与 reference identity 的使用形态。',
    source: 'spec/contracts/C-ANATOMY-0002.yaml',
  },
  {
    id: 'C-ANATOMY-0003',
    title: 'Setup family registration 是次级能力',
    summary:
      '`def.anatomy.family` 保留为 setup-only 注册路径，主要用于动态、局部、测试或 legacy 场景。',
    quote:
      '`def.anatomy.family` remains setup-only registration for local, dynamic, test, or legacy family declarations.',
    criteria: [
      '`def.anatomy.family` 只能在 setup 期间调用。',
      '稳定共享 family 应优先由 family token 携带 canonical spec。',
      '若 token 已携带 spec，claim 可以不依赖显式 setup 注册。',
    ],
    tests: 'T-ANATOMY-0001 覆盖 setup registration 与 embedded spec claim。',
    source: 'spec/contracts/C-ANATOMY-0003.yaml',
  },
  {
    id: 'C-ANATOMY-0004',
    title: 'Claim 声明当前实例承担某个 role',
    summary:
      '`def.anatomy.claim` 是 setup-only role 声明；它声明结构身份，不创建 part，也不注入行为。',
    quote:
      'An anatomy claim is a setup-only declaration that the current prototype instance takes one role in one family.',
    criteria: [
      '`def.anatomy.claim` 只能在 setup 期间调用。',
      'role 必须存在于对应 family spec 中。',
      '同一 instance 同一 family 只能 claim 一个 role。',
      'profile 只能由 root claim 指定。',
    ],
    tests:
      'T-ANATOMY-0001 覆盖 claim phase guard、duplicate claim、非法 profile 与 embedded spec claim。',
    source: 'spec/contracts/C-ANATOMY-0004.yaml',
  },
  {
    id: 'C-ANATOMY-0005',
    title: 'Domain 由 root claim 划定',
    summary:
      'runtime query 解析到当前实例所属的最近 root domain，并且只观察该 domain 内同 family part。',
    quote:
      'A root claim establishes the runtime domain in which same-family parts can observe each other.',
    criteria: [
      'root claim 是 anatomy domain 的作用域锚点。',
      'runtime query 限制在当前 instance 所属的最近合法 root domain 内。',
      '同一 family 多个 root 可以形成多个独立 domain。',
      '默认 strict query 在无法解析合法 domain 时必须失败。',
    ],
    tests: 'T-ANATOMY-0002 与 T-ANATOMY-ORDER-0001 覆盖 domain resolution 与 missing-domain 行为。',
    source: 'spec/contracts/C-ANATOMY-0005.yaml',
  },
  {
    id: 'C-ANATOMY-0006',
    title: 'Profile 是同 family 内的 refinement',
    summary:
      'profile 可以收紧 family 基线，但不是继承、派生或 override；差异过大时应定义新的 family。',
    quote:
      'A profile is a named refinement inside one family, not inheritance, derivation, or arbitrary override.',
    criteria: [
      'profile 建立在同一 family 的基线 spec 之上。',
      'profile 可以收紧 cardinality、增加 asHook requirement、增加更严格 relation。',
      'profile 不得放宽、删除或覆盖 family 基线约束。',
      'profile 不得解释为 family 继承或跨 family 多态。',
    ],
    tests: 'T-ANATOMY-0003 覆盖 profile refinement 与 family/profile diagnostic 分层。',
    source: 'spec/contracts/C-ANATOMY-0006.yaml',
  },
  {
    id: 'C-ANATOMY-0007',
    title: 'Requirement 检查 asHook 能力',
    summary: 'v0 requirement 只固化 asHook 检查；claim 是身份认领，requirement 是 role 能力诊断。',
    quote:
      'The v0 anatomy requirement form checks declared asHook capabilities for role claims without injecting behavior.',
    criteria: [
      'v0 requirement 的稳定形式只包括 asHook 检查。',
      'requirement 不自动调用 asHook，也不注入行为。',
      'family requirement 缺失是 family 级错误。',
      'profile 额外 requirement 缺失默认是 warning。',
    ],
    tests: 'T-ANATOMY-0003 覆盖 missing family hook 与 profile/family diagnostic 分层。',
    source: 'spec/contracts/C-ANATOMY-0007.yaml',
  },
  {
    id: 'C-ANATOMY-0008',
    title: 'Runtime query 返回安全 PartView',
    summary:
      '`run.anatomy.*` 是 runtime-only 查询面，只返回实际存在的 same-domain part view，且不得暴露 raw target。',
    quote:
      'Anatomy runtime queries are runtime-only and may return only limited part views for actual same-domain parts.',
    criteria: [
      'Anatomy runtime query API 只能在 runtime 期间调用。',
      'query 不得虚拟补全缺失 part。',
      'PartView 不得暴露 prototype instance、host node、root target 或 adapter target。',
      'renderer read view 只能提供 anatomy 只读 query surface。',
    ],
    tests: 'T-ANATOMY-0002 覆盖 runtime query、render read 与 PartView safety。',
    source: 'spec/contracts/C-ANATOMY-0008.yaml',
  },
  {
    id: 'C-ANATOMY-0009',
    title: 'PartView 只能读取显式 Expose',
    summary: 'Anatomy 可以读取 same-domain part 主动 expose 的能力，但不新增独立输出系统。',
    quote:
      'Anatomy may read only capabilities explicitly exposed by same-domain parts through Expose.',
    criteria: [
      'PartView 可以提供 `hasExpose` 与 `getExpose`。',
      'Anatomy 只能读取目标 part 已通过 Expose 主动公开的 key。',
      '目标未 expose 的能力不得被推断、补造或访问。',
      'Anatomy 对 Expose 的实现依赖必须在治理与 module 依赖中记录。',
    ],
    tests: 'T-ANATOMY-0002 与 adapter runtime-access contract 覆盖 same-domain expose read。',
    source: 'spec/contracts/C-ANATOMY-0009.yaml',
  },
  {
    id: 'C-ANATOMY-0010',
    title: 'Diagnostics 区分 family 与 profile 失败',
    summary: 'family 级违规默认是 error；profile 级违规默认是 warning，且不能降低 family 错误。',
    quote:
      'Anatomy diagnostics distinguish broken family baselines from unmet profile refinements.',
    criteria: [
      '非法 claim、缺失 domain、family cardinality/relation/requirement 失败属于 family 级问题。',
      'family 级问题默认报告为 error。',
      'profile cardinality/relation/requirement 失败默认报告为 warning。',
      '诊断必须区分 family required role 与 profile recommended/tightened role。',
    ],
    tests: 'T-ANATOMY-0003 覆盖 family error 与 profile warning。',
    source: 'spec/contracts/C-ANATOMY-0010.yaml',
  },
  {
    id: 'C-ANATOMY-ORDER-0001',
    title: 'Order 是宿主有序结构视图',
    summary:
      '`run.anatomy.order` 观察当前 domain 中 part 的宿主可见顺序，但不承担 collection 语义。',
    quote:
      '`run.anatomy.order` provides runtime-only host-ordered projections of same-domain parts without becoming collection semantics.',
    criteria: [
      '`run.anatomy.order` 只能在 runtime 期间读取。',
      'order view 以当前 domain 中实际存在的 part 为输入。',
      '宿主可提供真实顺序；否则可以退回稳定顺序。',
      'selection、active item、roving focus 与键盘策略不属于 anatomy.order。',
    ],
    tests: 'T-ANATOMY-ORDER-0001 覆盖 host order 与 order/collection 边界。',
    source: 'spec/contracts/C-ANATOMY-ORDER-0001.yaml',
  },
  {
    id: 'C-ANATOMY-ORDER-0002',
    title: 'Order 暴露 self position 与 version',
    summary:
      'order view 可以提供 ordered projection、self index、prev/next 与 order signature version。',
    quote:
      'Anatomy order can expose ordered role projections, self adjacency, and an order signature version.',
    criteria: [
      'order view 可以提供 `parts`、`partsOf`、`indexOfSelf`、`prevOfSelf`、`nextOfSelf`。',
      'self adjacency 必须基于同 domain、同 role 的有序投影。',
      '`version` 只表达 order signature 变化。',
      '`version` 不表达业务数据、Expose、State、Feedback 或 Template 内容变化。',
    ],
    tests: 'T-ANATOMY-ORDER-0001 覆盖 self index、prev/next 与 version。',
    source: 'spec/contracts/C-ANATOMY-ORDER-0002.yaml',
  },
];

## Anatomy 是什么？

`Anatomy` 是 Proto UI 用来描述复合原型结构语义的系统。它回答的问题是：当一个原型被拆成 root、trigger、content、item、indicator 等多个稳定 part 后，这些 part 如何被识别为同一个 family，分别承担什么 role，以及它们之间有什么结构关系。

Anatomy 不是信息通路。组件间共享数据仍然属于 `Context`；组件向 App Maker 承诺可访问能力属于 `Expose`；User 交互进入组件属于 `Event`；User 感知组件结果属于 `Feedback`。Anatomy 只解决复合原型的结构协作问题，尤其是单靠 Context 难以表达的 role、domain、order 与 relation。

它也不是装配器。Anatomy 不创建 part，不移动 part，不自动补全缺失结构，不改写 template，也不替原型作者组合其他原型。

## Family Spec

Anatomy family 是一套结构语义坐标系。稳定 family 应在模块静态位置创建：

```ts
import { createAnatomyFamily } from '@proto.ui/core';

export const SELECT_FAMILY = createAnatomyFamily('base-select', {
  roles: {
    root: { cardinality: { min: 1, max: 1 } },
    trigger: { cardinality: { min: 0, max: 1 } },
    content: { cardinality: { min: 0, max: 1 } },
    item: { cardinality: { min: 0, max: 100 } },
  },
  relations: [
    { kind: 'contains', parent: 'root', child: 'trigger' },
    { kind: 'contains', parent: 'root', child: 'content' },
    { kind: 'contains', parent: 'content', child: 'item' },
  ],
});
```

<SpecPhaseBadge label="static-only" />

`debugName` 只用于诊断、调试和工具展示。family identity 由 token 的引用身份决定，而不是由字符串名称决定。

`def.anatomy.family(family, spec)` 仍然存在，但它是 setup-only 的次级能力，主要用于动态、局部、测试或 legacy 场景。稳定库级 family 不应要求每个 part 重复调用 `register*Family(def)` helper。

## Claim 与 Domain

<SpecPhaseBadge label="setup-only" />

part 在 setup 期间通过 claim 声明自己承担某个 role：

```ts
def.anatomy.claim(SELECT_FAMILY, { role: 'trigger' });
```

claim 只声明结构身份。它不会创建 part，不会注入行为，也不会替代 asHook 调用。同一个 prototype instance 在同一个 family 中只能 claim 一个 role，且 role 必须存在于 family spec 中。

root claim 划定一个 anatomy domain：

```ts
def.anatomy.claim(SELECT_FAMILY, { role: 'root' });
```

runtime query 只在当前 instance 所属的最近 root domain 中解析。同一个 family 可以在不同位置形成多个独立 domain；查询不得跨 domain 混合 part。

## Profile

profile 是同一个 family 内的具名 refinement。它适合表达“这个 family 在某个使用形态下需要更严格的结构”，但它不是继承、派生或 override。

profile 可以：

- 收紧 cardinality
- 增加 asHook requirement
- 增加更严格 relation

profile 不可以：

- 放宽 family cardinality
- 删除 family requirement
- 移除 family relation
- 把一个 family 变成另一个 family

如果差异无法用“收紧”表达，就应该定义新的 anatomy family，而不是在 family 上建立一套继承链。

## Requirement 与 Diagnostics

v0 稳定的 Anatomy requirement 只有 asHook 检查：

```ts
roles: {
  trigger: {
    cardinality: { min: 0, max: 1 },
    requires: [{ kind: 'hook', name: 'asTrigger' }],
  },
}
```

claim 表示“我是这个 role”；requirement 表示“承担这个 role 的 prototype 应具备某种行为协议能力”。requirement 不会自动调用 asHook，也不会注入行为。

diagnostics 区分两层：

| 层级    | 默认等级 | 含义                                       |
| ------- | -------- | ------------------------------------------ |
| family  | error    | family 的核心结构语义不成立                |
| profile | warning  | 某个具名 refinement 的推荐或收紧结构未满足 |

非法 claim、缺失合法 domain、family cardinality/relation/requirement 失败都属于 family 级问题。profile 额外 cardinality、relation 或 requirement 未满足，默认是 warning。

## Runtime Query 与 PartView

<SpecPhaseBadge label="runtime-only" />

runtime 期间可以查询当前 domain 中实际存在的 part：

```ts
const trigger = run.anatomy.partsOf(SELECT_FAMILY, 'trigger')[0] ?? null;
const hasContent = run.anatomy.has(SELECT_FAMILY, 'content');
```

query 返回的是受限 `PartView`，不是 prototype instance，也不是 host node。PartView 不得暴露 root target、adapter target 或任何 raw reference。

PartView 可以读取目标 part 显式 expose 的能力：

```ts
const close = trigger?.getExpose('close');
```

如果目标 part 没有 expose 某个 key，Anatomy 不会推断、补造或访问内部替代能力。Anatomy 因此在实现上依赖 Expose，但这种依赖是“使用 Expose”，不是继承 Expose 的信息通路身份。

## Order View

`run.anatomy.order` 是 Anatomy 的宿主有序结构视图。它回答的是：当前 domain 中这些 part 按宿主可观察顺序排列时是什么样。

```ts
const items = run.anatomy.order.partsOf(SELECT_FAMILY, 'item');
const index = run.anatomy.order.indexOfSelf(SELECT_FAMILY, 'item');
const prev = run.anatomy.order.prevOfSelf(SELECT_FAMILY, 'item');
const next = run.anatomy.order.nextOfSelf(SELECT_FAMILY, 'item');
```

order view 不等于 collection。它不负责 item metadata、selection、active item、roving focus 或键盘策略。上层 asHook 可以基于 anatomy.order 组织 collection 语义，但 anatomy.order 自身只提供结构顺序投影。

`version()` 表示 order signature 变化，不表示业务数据、Expose 值、State 值、Feedback 结果或 Template 内容变化。

## Query Policy

普通作者侧 runtime query 默认是 strict：当前 instance 如果无法解析到合法 domain，query 应该失败。

实现内部保留 missing-domain query policy，用于少数 privileged 结构投影，例如 `asCollection` 在临时结构窗口中返回 `null` 或 `[]`。这不是 `context.try*` 那种语义可选性，也不表示 anatomy 关系本身可选。

## 契约实体预览

<SpecContractPreview items={contractPreviews} locale="zh-cn" />

## 与测试的关系

Anatomy 覆盖映射到以下测试实体：

| 测试实体               | 主要覆盖                                                              |
| ---------------------- | --------------------------------------------------------------------- |
| `T-ANATOMY-0001`       | family spec、setup registration、claim 基础规则                       |
| `T-ANATOMY-0002`       | domain resolution、runtime query、PartView safety、Expose 读取        |
| `T-ANATOMY-0003`       | profile refinement、asHook requirement、diagnostics                   |
| `T-ANATOMY-ORDER-0001` | order view、self index、prev/next、version、privileged missing policy |

这些测试把 Anatomy 从“复合组件内部能互相找一下”收敛为可验证边界：family 如何定义，part 如何 claim，domain 如何划分，runtime 能看见什么，PartView 不能泄漏什么，以及 order view 到底承担哪一层语义。

## 与其他规范的关系

- `Core` 提供 `createAnatomyFamily`、setup/runtime phase、render read view 与共享类型边界。
- `Context` 是官方组件间信息通路；Anatomy 是结构语义补全，不替代 Context。
- `Expose` 是 Component → App Maker 信息通路；Anatomy 只能读取 part 主动 expose 的能力。
- `Template` 决定 render output；Anatomy 不创建、不移动、不补全 template 结构。
- `asHook` 可以提供 role 所需的行为协议能力；Anatomy requirement 只诊断是否具备这些能力。
- `Lifecycle` 定义 family/claim、runtime query、order query 与 cleanup 的可用时机。
