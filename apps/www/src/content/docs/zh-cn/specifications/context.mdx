---
title: 'Context'
desp: 'Context 规范'
description: 'Context 规范'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-CONTEXT-0001',
    title: 'Context 是官方组件间信息通路',
    summary:
      'Context 是 Proto UI 中 Component 与 Component 之间共享信息的正式通路，其他信息通路不负责组件间通信。',
    quote: 'Context is the official Component-to-Component information channel in Proto UI.',
    criteria: [
      'Context 必须被识别为 Component ↔ Component 信息通路。',
      '除 Context 外，其他信息通路不得被规范为组件间通信机制。',
      'Anatomy + Expose 可以作为兜底组合能力，但不是官方信息通路。',
    ],
    tests: 'T-CONTEXT-0001 通过 provider/consumer scope 行为覆盖 Context channel 定位。',
    source: 'spec/contracts/C-CONTEXT-0001.yaml',
  },
  {
    id: 'C-CONTEXT-0002',
    title: 'Provider 建立通信作用域而不是单向数据流',
    summary:
      'provider 负责建立 ContextKey 的 scope 与初始值；同一 resolved scope 内的组件可以读取、订阅并发起更新。',
    quote: 'A Context provider establishes the communication scope and initial value.',
    criteria: [
      'provider 必须建立 ContextKey 对应的 scope 与初始 context value。',
      'provider 不代表 context 只能 provider → consumer 单向传播。',
      '已订阅同一 resolved context scope 的组件可以通过 runtime API 更新该 context。',
    ],
    tests: 'T-CONTEXT-0001 与 T-CONTEXT-0002 覆盖 provider scope 与 consumer update 行为。',
    source: 'spec/contracts/C-CONTEXT-0002.yaml',
  },
  {
    id: 'C-CONTEXT-0003',
    title: 'ContextKey identity 由 core 创建',
    summary:
      'ContextKey 是 context channel 的稳定身份 token，必须按引用比较，并通过 core factory 创建。',
    quote:
      'ContextKey is the identity token for a context channel and must be compared by reference identity.',
    criteria: [
      'ContextKey identity 必须按引用比较。',
      '`debugName` 不得参与 identity、provider resolution 或 subscription matching。',
      'core 必须提供 `createContextKey<T>(debugName)` 一类工厂函数。',
    ],
    tests: 'T-CONTEXT-0001 key identity case 覆盖 factory 与引用身份。',
    source: 'spec/contracts/C-CONTEXT-0003.yaml',
  },
  {
    id: 'C-CONTEXT-0004',
    title: 'Context resolution 绑定最近 scope owner',
    summary:
      'Context resolution 基于 runtime/adapter 提供的 scope ancestry；对同一个 ContextKey，最近 provider wins。',
    quote:
      'Context resolution binds a participant to the nearest provider or equivalent scope owner.',
    criteria: [
      'resolution 必须基于 runtime/adapter 提供的 scope ancestry。',
      '同一个 ContextKey 可以被多个组件实例同时 provide。',
      '同一 key 存在多个 provider 时，最近的 scope owner wins。',
    ],
    tests: 'T-CONTEXT-0001 scope resolution case 覆盖最近 provider 绑定。',
    source: 'spec/contracts/C-CONTEXT-0004.yaml',
  },
  {
    id: 'C-CONTEXT-0005',
    title: 'Provide 是 setup-only 并安装初始值',
    summary:
      '`def.context.provide(key, defaultValue)` 在 setup 期间建立当前组件实例上的 context scope。',
    quote: '`def.context.provide(key, defaultValue)` is a setup-only API.',
    criteria: [
      '`def.context.provide(...)` 必须是 setup-only。',
      '同一组件实例不得重复 provide 同一个 ContextKey。',
      '返回的 provider update function 只能在 runtime callback 中调用。',
    ],
    tests: 'T-CONTEXT-0001 provide value case 覆盖 setup provider 与 value boundary。',
    source: 'spec/contracts/C-CONTEXT-0005.yaml',
  },
  {
    id: 'C-CONTEXT-0006',
    title: 'Subscribe 与 trySubscribe 表达不同依赖强度',
    summary:
      '`subscribe` 声明必需 context；`trySubscribe` 声明可选协作环境，缺失 provider 时组件仍需自治运行。',
    quote:
      '`subscribe` declares a required context dependency, while `trySubscribe` declares an optional context dependency.',
    criteria: [
      '`def.context.subscribe(key, cb?)` 是 setup-only。',
      '`def.context.trySubscribe(key, cb?)` 是 setup-only。',
      'required subscribe 在 setup 期间无法解析 provider 时必须失败。',
      'optional trySubscribe 在 provider 不存在时不得失败。',
    ],
    tests: 'T-CONTEXT-0002 覆盖 required/optional subscription intent。',
    source: 'spec/contracts/C-CONTEXT-0006.yaml',
  },
  {
    id: 'C-CONTEXT-0007',
    title: 'Read surface 遵守 subscription intent',
    summary:
      '`read` 读取 required context；`tryRead` 读取 optional context，缺失或断开时返回 `null`。',
    quote: 'Context read surfaces must follow the subscription intent declared during setup.',
    criteria: [
      '`read(key)` 必须要求 prior `subscribe(key)`。',
      '`tryRead(key)` 必须要求 prior `trySubscribe(key)`。',
      'render read surface 只能暴露 read/tryRead，不得暴露 update/tryUpdate。',
    ],
    tests: 'T-CONTEXT-0002 覆盖 runtime read 与 render read surface。',
    source: 'spec/contracts/C-CONTEXT-0007.yaml',
  },
  {
    id: 'C-CONTEXT-0008',
    title: 'Runtime update 必须显式指定 key 与 next value',
    summary:
      '`run.context.update(key, next)` 表达必定存在的 context 更新；`tryUpdate(key, next)` 表达可选 context 更新。',
    quote: 'Runtime context updates must explicitly specify the ContextKey and next context value.',
    criteria: [
      '`run.context.update(key, next)` 必须显式接收 ContextKey 与 next value/updater。',
      '`run.context.tryUpdate(key, next)` 必须显式接收 ContextKey 与 next value/updater。',
      '`tryUpdate` 在 context 不存在时返回 `false`，成功更新时返回 `true`。',
    ],
    tests: 'T-CONTEXT-0002 runtime update case 覆盖 update/tryUpdate split。',
    source: 'spec/contracts/C-CONTEXT-0008.yaml',
  },
  {
    id: 'C-CONTEXT-0009',
    title: 'Context value 是顶层 JSON Object',
    summary:
      'context value 必须是 JSON-compatible plain object；顶层 `null` 保留给 optional absence。',
    quote: 'Context values must be JSON-compatible objects at the top level.',
    criteria: [
      'context value 顶层必须是 plain object。',
      '顶层 `null` 不得作为实际 context value。',
      '嵌套值必须保持 JSON-compatible。',
    ],
    tests: 'T-CONTEXT-0001 value boundary case 覆盖 plain object 与非法值。',
    source: 'spec/contracts/C-CONTEXT-0009.yaml',
  },
  {
    id: 'C-CONTEXT-0010',
    title: 'Context callback 观察语义更新',
    summary:
      'context callback 必须观察成功更新后的 next/prev value；adapter 可以优化调度但不能改变语义顺序。',
    quote: 'Context callbacks observe semantic context updates deterministically.',
    criteria: [
      'successful update 必须向当前 resolved scope 内的 active subscriptions 派发 callback。',
      'callback 必须接收 next 与 prev context value。',
      'adapter 可优化 scheduling，但不得改变可观察语义顺序。',
    ],
    tests: 'T-CONTEXT-0002 callback delivery case 覆盖 next/prev 与 dispatch。',
    source: 'spec/contracts/C-CONTEXT-0010.yaml',
  },
  {
    id: 'C-CONTEXT-0011',
    title: 'Rebinding 与 disconnected 在 read/update 时解析',
    summary:
      'scope ancestry 变化可能重新绑定 provider 或断开 context；v0 不提供 connected/disconnected notification API。',
    quote:
      'Changes in context scope ancestry may rebind participants to different providers or disconnect a context.',
    criteria: [
      'read/update 必须按当前 scope ancestry 解析 provider。',
      'required read/update disconnected 时必须失败。',
      'optional tryRead/tryUpdate disconnected 时必须返回 `null` 或 `false`。',
    ],
    tests: 'T-CONTEXT-0002 reconnect/cleanup case 覆盖 rebind 与 disconnected。',
    source: 'spec/contracts/C-CONTEXT-0011.yaml',
  },
  {
    id: 'C-CONTEXT-0012',
    title: 'Context entry 随 lifecycle 清理',
    summary:
      'component instance 离开 runtime responsibility 后，context provider、subscription 与 callback 不得继续存活。',
    quote: 'Context participation must be scoped to the component instance lifecycle.',
    criteria: [
      'dispose 必须移除该实例 provide 的 context entries。',
      'dispose 必须移除该实例创建的 subscriptions。',
      'cleanup 后旧 callback 不得继续被调用。',
    ],
    tests: 'T-CONTEXT-0002 reconnect/cleanup case 覆盖 lifecycle cleanup。',
    source: 'spec/contracts/C-CONTEXT-0012.yaml',
  },
];

## Context 是什么？

`Context` 是 Proto UI 的 Component ↔ Component 信息通路。它让一组处在同一通信作用域内的组件共享一个由 `ContextKey` 标识的 JSON object value。

Context 是官方承认的组件间通信路径。`Props`、`Event`、`Feedback`、`Expose` 各自有明确方向，不能被扩展成组件之间的通用信息传递机制。`Anatomy + Expose` 可以在少数场景中作为兜底方案，但它不是 Proto UI 鼓励的首要组件间通信手段。

Context 的设计更接近“组件实例之间共享一个 scoped channel”，而不是 React 或 Vue 里单纯从 provider 往 consumer 下发数据的模型。provider 负责建立 scope 和初始值，但不垄断更新权。

## ContextKey

Context channel 由 `ContextKey` 标识。key 的 identity 按引用比较，而不是按字符串名称、`debugName` 或对象结构比较。

```ts
import { createContextKey } from '@proto.ui/core';

type TabsContextValue = {
  value: string;
  activeValue: string;
};

export const TABS_CONTEXT = createContextKey<TabsContextValue>('base-tabs');
```

`debugName` 只服务于诊断、错误信息和调试展示。两个 `debugName` 相同但引用不同的 `ContextKey` 是两个不同的 context channel。

## Provide 与 Scope

<SpecPhaseBadge label="setup-only" />

provider 在 setup 期间建立 context scope：

```ts
const updateTabs = def.context.provide(TABS_CONTEXT, {
  value: '',
  activeValue: '',
});
```

`def.context.provide(key, defaultValue)` 是 setup-only。它把当前组件实例注册为该 key 的 scope owner，并安装初始 context value。

同一个组件实例不能重复 provide 同一个 key。不同组件实例可以同时 provide 同一个 key；consumer 解析时绑定到当前 scope ancestry 中最近的 provider。常见宿主会用组件树或逻辑树表达 ancestry；如果宿主不是树形结构，adapter 也可以提供等价的 scope resolution 逻辑。

`provide` 返回的 update function 是 provider 侧的 runtime callback API，只能在 runtime callback 中调用：

```ts
def.lifecycle.onMounted(() => {
  updateTabs((prev) => ({ ...prev, activeValue: prev.value }));
});
```

## Subscribe 与 Try Subscribe

Context 订阅 API 在 setup 期间声明组件如何参与某个 context scope。

```ts
def.context.subscribe(TABS_CONTEXT, (run, next, prev) => {
  // required dependency: provider 必须存在
});

def.context.trySubscribe(TABS_CONTEXT, (run, next, prev) => {
  // optional cooperation: provider 可能不存在
});
```

`subscribe(key, cb?)` 表示该 context 是组件成立所必需的。如果 setup 期间无法解析 provider，组件应该直接失败，因为没有这个 context 它无法完成基本交互。

`trySubscribe(key, cb?)` 表示该组件可以在某个 context 存在时参与协作，也可以在它不存在时自治运行。optional context 缺失不是错误；原型作者必须为缺失场景负责。

这两个 API 的分离是刻意的。它让原型作者明确表达“我一定依赖这个 context”还是“我可以尝试加入一个可能存在的协作环境”。

## Runtime Read 与 Update

Context runtime surface 遵守 setup 期间声明过的 subscription intent：

| API                                | 需要的 setup intent                     | 缺失 provider 时 |
| ---------------------------------- | --------------------------------------- | ---------------- |
| `run.context.read(key)`            | `subscribe(key)`                        | throw            |
| `run.context.tryRead(key)`         | `trySubscribe(key)`                     | `null`           |
| `run.context.update(key, next)`    | `subscribe(key)` 或 `trySubscribe(key)` | throw            |
| `run.context.tryUpdate(key, next)` | `trySubscribe(key)`                     | `false`          |

runtime update 必须显式带上 `ContextKey` 与 next value 或 updater：

```ts
run.context.update(TABS_CONTEXT, (prev) => ({
  ...prev,
  value: nextValue,
}));
```

这个 API 不依赖某个隐式“当前 context”。同一组件可以订阅多个 context，显式 key 能避免更新目标含糊。

## Context Value

Context value 的顶层必须是 JSON-compatible plain object。

```ts
def.context.provide(TABS_CONTEXT, {
  value: '',
  activeValue: '',
});
```

顶层 `null` 不允许作为实际 context value，因为 `tryRead` 用 `null` 表示 optional context 当前不可用。嵌套值仍然必须保持 JSON-compatible：string、number、boolean、`null`、array 和 plain object 可以出现；function、symbol、class instance、Date、DOM object 或循环引用都不属于 portable context value。

## Rebinding 与 Cleanup

Context resolution 在 read/update 时按当前 scope ancestry 解析。这意味着组件树或等价 scope 关系变化后，consumer 可能重新绑定到另一个 provider，也可能进入 disconnected 状态。

v0 不提供 connected/disconnected notification API。required read/update 在 disconnected 时失败；optional tryRead/tryUpdate 用 `null` 或 `false` 表示当前不可用。

Context participation 绑定到组件实例 lifecycle。当实例 dispose 时，它提供的 context entries、创建的 subscriptions 和 pending callbacks 都必须被清理。cleanup 后旧 callback 不得继续响应新的 context update。

## 契约实体预览

<SpecContractPreview items={contractPreviews} locale="zh-cn" />

## 与测试的关系

Context 覆盖映射到以下测试实体：

| 测试实体 | 主要覆盖 |
| --- | --- |
| `T-CONTEXT-0001` | ContextKey identity、provider scope、scope resolution、value boundary |
| `T-CONTEXT-0002` | required/optional subscription、read/update、callback delivery、render read、rebind/cleanup |

这些测试把 Context 从“组件树上的共享数据”收敛为一组可验证边界：谁建立 scope、谁能订阅、何时可读写、value 如何保持 portable，以及 scope 变化和 lifecycle cleanup 如何影响可见行为。

## 与其他规范的关系

- `Core` 定义信息通路、setup/runtime、JSON value boundary 与 `createContextKey`。
- `Props` 是 App Maker → Component 的配置输入；Context 是 Component ↔ Component 的 scoped cooperation。
- `Expose` 是 Component → App Maker；通过 anatomy 取得另一个组件 expose 可以作为兜底能力，但不是官方组件间信息通路。
- `Event` 是 User → Component；context 可以在 event callback 中被读取或更新，但 event 不负责组件间通信。
- `State` 表达组件内部连续性；context value 可以包含状态快照，但 Context 自身不是 state slot。
- `Lifecycle` 定义 provide、subscribe、runtime callback、read/update 与 cleanup 的可用边界。
