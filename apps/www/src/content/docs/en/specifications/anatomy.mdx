---
title: 'Anatomy'
desp: 'Anatomy specification'
description: 'Anatomy specification'
---

import SpecContractPreview from '@/components/SpecContractPreview.astro';
import SpecPhaseBadge from '@/components/SpecPhaseBadge.astro';

export const contractPreviews = [
  {
    id: 'C-ANATOMY-0001',
    title: 'Anatomy is structural semantics, not an information channel',
    summary:
      'Anatomy describes roles, relations, constraints, and limited observation inside a compound prototype family; it is not a general communication mechanism.',
    quote: 'Anatomy is not a Proto UI information channel.',
    criteria: [
      'Anatomy is a structural-semantic system, not an additional information channel.',
      'Anatomy does not create, move, or materialize parts, and does not rewrite template or host trees.',
      'General inter-component communication remains Context.',
    ],
    tests:
      'Anatomy positioning is primarily constrained by contract text; runtime behavior is indirectly covered by T-ANATOMY-0002 and T-ANATOMY-ORDER-0001.',
    source: 'spec/contracts/C-ANATOMY-0001.yaml',
  },
  {
    id: 'C-ANATOMY-0002',
    title: 'Family specs are static definitions',
    summary:
      'A stable anatomy family is identified by a reference-identity family token that carries its canonical spec, including the root role.',
    quote:
      'A stable anatomy family is defined by a reference-identity family token that carries its canonical spec, including the required root role.',
    criteria: [
      '`createAnatomyFamily(debugName, spec)` is the static-only API for stable family specs.',
      'Family identity is determined by token reference, not by `debugName`.',
      'The family spec must declare the `root` role.',
      'Stable library families should not require each part to register the same spec during setup.',
    ],
    tests:
      'T-ANATOMY-0001 covers static family specs, the root-role requirement, and the reference-identity usage shape.',
    source: 'spec/contracts/C-ANATOMY-0002.yaml',
  },
  {
    id: 'C-ANATOMY-0003',
    title: 'Setup family registration is not author-facing',
    summary:
      '`def.anatomy.family` is not part of the prototype-author API; family specs must be carried by static tokens.',
    quote:
      '`def.anatomy.family` is not part of the prototype-author API. Anatomy family specs must be carried by static-only family tokens.',
    criteria: [
      '`DefHandle.anatomy` must not expose a `family(...)` registration API.',
      'Setup must not repeat family-spec registration through `register*Family(def)` helpers.',
      'Claims must resolve roles, profiles, relations, and requirements from the canonical spec carried by the family token.',
    ],
    tests: 'T-ANATOMY-0001 covers no setup registration and static-spec claims.',
    source: 'spec/contracts/C-ANATOMY-0003.yaml',
  },
  {
    id: 'C-ANATOMY-0004',
    title: 'Claims declare roles',
    summary:
      '`def.anatomy.claim` is a setup-only role declaration; it declares structural identity without creating parts or injecting behavior.',
    quote:
      'An anatomy claim is a setup-only declaration that the current prototype instance takes one role in one family.',
    criteria: [
      '`def.anatomy.claim` is setup-only.',
      'The role must exist in the corresponding family spec.',
      'One instance may claim only one role in the same family.',
      'Only root claims may specify a profile.',
    ],
    tests:
      'T-ANATOMY-0001 covers claim phase guards, duplicate claims, invalid profiles, and embedded-spec claims.',
    source: 'spec/contracts/C-ANATOMY-0004.yaml',
  },
  {
    id: 'C-ANATOMY-0005',
    title: 'Domains are scoped by root claims',
    summary:
      'Runtime queries resolve to the nearest root domain containing the current instance and observe same-family parts only inside that domain.',
    quote:
      'A root claim establishes the runtime domain in which same-family parts can observe each other.',
    criteria: [
      'A root claim is the scope anchor for an anatomy domain.',
      'Runtime queries are limited to the nearest valid root domain containing the current instance.',
      'Multiple roots of the same family form independent domains.',
      'Default strict queries fail when no valid domain resolves.',
    ],
    tests:
      'T-ANATOMY-0002 and T-ANATOMY-ORDER-0001 cover domain resolution and missing-domain behavior.',
    source: 'spec/contracts/C-ANATOMY-0005.yaml',
  },
  {
    id: 'C-ANATOMY-0006',
    title: 'Profiles refine a family',
    summary:
      'Profiles tighten a family baseline, but are not inheritance, derivation, or override; large differences should use a new family.',
    quote:
      'A profile is a named refinement inside one family, not inheritance, derivation, or arbitrary override.',
    criteria: [
      'Profiles are based on the baseline spec of the same family.',
      'Profiles may tighten cardinality, add asHook requirements, and add stricter relations.',
      'Profiles must not relax, delete, or override baseline family constraints.',
      'Profiles must not be interpreted as family inheritance or cross-family polymorphism.',
    ],
    tests: 'T-ANATOMY-0003 covers profile refinement and family/profile diagnostic layering.',
    source: 'spec/contracts/C-ANATOMY-0006.yaml',
  },
  {
    id: 'C-ANATOMY-0007',
    title: 'Requirements check asHook capabilities',
    summary:
      'v0 requirements only stabilize asHook checks; claims declare identity, while requirements diagnose role capability expectations.',
    quote:
      'The v0 anatomy requirement form checks declared asHook capabilities for role claims without injecting behavior.',
    criteria: [
      'The stable v0 requirement form includes only asHook checks.',
      'Requirements do not automatically call asHooks and do not inject behavior.',
      'Missing family requirements are family-level errors.',
      'Missing additional profile requirements default to warnings.',
    ],
    tests: 'T-ANATOMY-0003 covers missing family hooks and profile/family diagnostic layering.',
    source: 'spec/contracts/C-ANATOMY-0007.yaml',
  },
  {
    id: 'C-ANATOMY-0008',
    title: 'Runtime queries return safe PartViews',
    summary:
      '`run.anatomy.*` is a runtime-only query surface that returns actual same-domain part views without exposing raw targets.',
    quote:
      'Anatomy runtime queries are runtime-only and may return only limited part views for actual same-domain parts.',
    criteria: [
      'Anatomy runtime query APIs are runtime-only.',
      'Queries must not virtually materialize missing parts.',
      'PartViews must not expose prototype instances, host nodes, root targets, or adapter targets.',
      'Renderer read view may expose anatomy read-only query surface only.',
    ],
    tests: 'T-ANATOMY-0002 covers runtime queries, render read, and PartView safety.',
    source: 'spec/contracts/C-ANATOMY-0008.yaml',
  },
  {
    id: 'C-ANATOMY-0009',
    title: 'PartViews read explicit Exposes only',
    summary:
      'Anatomy may read capabilities explicitly exposed by same-domain parts, but does not create an independent output system.',
    quote:
      'Anatomy may read only capabilities explicitly exposed by same-domain parts through Expose.',
    criteria: [
      'PartViews may provide `hasExpose` and `getExpose`.',
      'Anatomy may read only keys explicitly exposed by the target part through Expose.',
      'Unexposed capabilities must not be inferred, fabricated, or accessed.',
      'Anatomy implementation dependency on Expose must be recorded in governance and module dependencies.',
    ],
    tests: 'T-ANATOMY-0002 and adapter runtime-access contracts cover same-domain expose reads.',
    source: 'spec/contracts/C-ANATOMY-0009.yaml',
  },
  {
    id: 'C-ANATOMY-0010',
    title: 'Diagnostics separate family and profile failures',
    summary:
      'Family violations default to errors; profile violations default to warnings and cannot downgrade family errors.',
    quote:
      'Anatomy diagnostics distinguish broken family baselines from unmet profile refinements.',
    criteria: [
      'Invalid claims, missing domains, and family cardinality/relation/requirement failures are family-level problems.',
      'Family-level problems default to errors.',
      'Profile cardinality/relation/requirement failures default to warnings.',
      'Diagnostics distinguish family required roles from profile recommended/tightened roles.',
    ],
    tests: 'T-ANATOMY-0003 covers family errors and profile warnings.',
    source: 'spec/contracts/C-ANATOMY-0010.yaml',
  },
  {
    id: 'C-ANATOMY-ORDER-0001',
    title: 'Order is a host-ordered structural view',
    summary:
      '`run.anatomy.order` observes host-visible order for parts in the current domain without taking on collection semantics.',
    quote:
      '`run.anatomy.order` provides runtime-only host-ordered projections of same-domain parts without becoming collection semantics.',
    criteria: [
      '`run.anatomy.order` is runtime-only.',
      'The order view uses actual parts in the current domain as input.',
      'Hosts may provide real order or fall back to a stable order.',
      'Selection, active item, roving focus, and keyboard policy are not anatomy.order responsibilities.',
    ],
    tests: 'T-ANATOMY-ORDER-0001 covers host order and the order/collection boundary.',
    source: 'spec/contracts/C-ANATOMY-ORDER-0001.yaml',
  },
  {
    id: 'C-ANATOMY-ORDER-0002',
    title: 'Order exposes self position and version',
    summary:
      'The order view may expose ordered projections, self index, previous/next parts, and an order signature version.',
    quote:
      'Anatomy order can expose ordered role projections, self adjacency, and an order signature version.',
    criteria: [
      'The order view may provide `parts`, `partsOf`, `indexOfSelf`, `prevOfSelf`, and `nextOfSelf`.',
      'Self adjacency is based on same-domain, same-role ordered projection.',
      '`version` represents only order signature changes.',
      '`version` does not represent business data, Expose, State, Feedback, or Template content changes.',
    ],
    tests: 'T-ANATOMY-ORDER-0001 covers self index, prev/next, and version.',
    source: 'spec/contracts/C-ANATOMY-ORDER-0002.yaml',
  },
];

## What Anatomy Is

`Anatomy` is Proto UI's structural-semantic system for compound prototypes. It answers: after a prototype is split into stable parts such as root, trigger, content, item, or indicator, how are those parts recognized as one family, what roles do they take, and what structural relations exist between them?

Anatomy is not an information channel. Shared data between components remains `Context`; component-to-App-Maker capabilities remain `Expose`; user interaction entering the component remains `Event`; user-perceivable output remains `Feedback`. Anatomy handles structural cooperation in compound prototypes, especially role, domain, order, and relation facts that Context alone would express poorly.

It is not an assembler either. Anatomy does not create parts, move parts, materialize missing structure, rewrite templates, or compose other prototypes for the author.

## Family Specs

An anatomy family is a structural-semantic coordinate system. Stable families should be created at module-static time:

```ts
import { createAnatomyFamily } from '@proto.ui/core';

export const SELECT_FAMILY = createAnatomyFamily('base-select', {
  roles: {
    root: { cardinality: { min: 1, max: 1 } },
    trigger: { cardinality: { min: 0, max: 1 } },
    content: { cardinality: { min: 0, max: 1 } },
    item: { cardinality: { min: 0, max: 100 } },
  },
  relations: [
    { kind: 'contains', parent: 'root', child: 'trigger' },
    { kind: 'contains', parent: 'root', child: 'content' },
    { kind: 'contains', parent: 'content', child: 'item' },
  ],
});
```

<SpecPhaseBadge label="static-only" />

`debugName` is only for diagnostics, debugging, and tooling display. Family identity is the token reference, not the string name.

The family spec must declare the `root` role. A root claim is the scope anchor for an anatomy domain; without root, runtime queries have no stable scope.

`def.anatomy.family(family, spec)` is not part of the prototype-author API. Stable library families should not require every part to call a `register*Family(def)` helper; parts only claim family tokens that already carry canonical specs.

## Claims And Domains

<SpecPhaseBadge label="setup-only" />

A part claims its role during setup:

```ts
def.anatomy.claim(SELECT_FAMILY, { role: 'trigger' });
```

A claim declares structural identity only. It does not create the part, inject behavior, or replace asHook calls. One prototype instance may claim only one role in the same family, and the role must exist in the family spec.

A root claim scopes an anatomy domain:

```ts
def.anatomy.claim(SELECT_FAMILY, { role: 'root' });
```

Runtime queries resolve inside the nearest root domain that contains the current instance. The same family may have several independent domains in different positions; queries must not mix parts across domains.

## Profiles

A profile is a named refinement inside the same family. It expresses "this use of the family expects a stricter structure," but it is not inheritance, derivation, or override.

A profile may:

- tighten cardinality
- add asHook requirements
- add stricter relations

A profile may not:

- relax family cardinality
- delete family requirements
- remove family relations
- turn one family into another family

If a difference cannot be expressed as tightening, define a new anatomy family instead of building an inheritance chain on the family.

## Requirements And Diagnostics

The only stable Anatomy requirement form in v0 is an asHook check:

```ts
roles: {
  trigger: {
    cardinality: { min: 0, max: 1 },
    requires: [{ kind: 'hook', name: 'asTrigger' }],
  },
}
```

A claim says "I am this role." A requirement says "a prototype taking this role is expected to have this behavioral protocol capability." Requirements do not call asHooks automatically and do not inject behavior.

Diagnostics have two levels:

| Level   | Default severity | Meaning                                 |
| ------- | ---------------- | --------------------------------------- |
| family  | error            | the core family structure does not hold |
| profile | warning          | a named refinement is not satisfied     |

Invalid claims, missing valid domains, and family cardinality/relation/requirement failures are family-level problems. Missing profile cardinality, relation, or additional requirement defaults to a warning.

## Runtime Query And PartView

<SpecPhaseBadge label="runtime-only" />

At runtime, a component may query actual parts in the current domain:

```ts
const trigger = run.anatomy.partsOf(SELECT_FAMILY, 'trigger')[0] ?? null;
const hasContent = run.anatomy.has(SELECT_FAMILY, 'content');
```

Queries return limited `PartView` objects, not prototype instances or host nodes. A PartView must not expose root targets, adapter targets, or any raw references.

A PartView may read capabilities explicitly exposed by the target part:

```ts
const close = trigger?.getExpose('close');
```

If the target part did not expose a key, Anatomy does not infer, fabricate, or access an internal substitute. Anatomy therefore depends on Expose in implementation, but that dependency is "usage of Expose," not inheritance of Expose's information-channel identity.

## Order View

`run.anatomy.order` is Anatomy's host-ordered structural view. It answers how parts in the current domain appear in host-observable order.

```ts
const items = run.anatomy.order.partsOf(SELECT_FAMILY, 'item');
const index = run.anatomy.order.indexOfSelf(SELECT_FAMILY, 'item');
const prev = run.anatomy.order.prevOfSelf(SELECT_FAMILY, 'item');
const next = run.anatomy.order.nextOfSelf(SELECT_FAMILY, 'item');
```

The order view is not collection semantics. It is not responsible for item metadata, selection, active item, roving focus, or keyboard policy. Higher-level asHooks may build collection semantics on top of anatomy.order, but anatomy.order itself only provides structural order projection.

`version()` represents changes to the order signature. It does not represent business data, Expose values, State values, Feedback results, or Template content changes.

## Query Policy

Ordinary author-facing runtime queries are strict by default: if the current instance cannot resolve a valid domain, the query should fail.

The implementation keeps missing-domain query policy for a few privileged structural projections, such as `asCollection` returning `null` or `[]` during transient structural windows. This is not semantic optionality like `context.try*`, and it does not mean the anatomy relationship itself is optional.

## Contract Previews

<SpecContractPreview items={contractPreviews} locale="en" />

## Test Mapping

Anatomy coverage is mapped through these test entities:

| Test entity            | Main coverage                                                         |
| ---------------------- | --------------------------------------------------------------------- |
| `T-ANATOMY-0001`       | family spec, setup registration, claim rules                          |
| `T-ANATOMY-0002`       | domain resolution, runtime query, PartView safety, Expose reads       |
| `T-ANATOMY-0003`       | profile refinement, asHook requirements, diagnostics                  |
| `T-ANATOMY-ORDER-0001` | order view, self index, prev/next, version, privileged missing policy |

These tests turn Anatomy from "compound parts can find each other somehow" into verifiable boundaries: how families are defined, how parts claim roles, how domains are scoped, what runtime may observe, what PartView must not leak, and exactly which semantic layer the order view owns.

## Related Specs

- `Core` provides `createAnatomyFamily`, setup/runtime phases, renderer read view, and shared type boundaries.
- `Context` is the official inter-component information channel; Anatomy is structural-semantic complement, not a replacement for Context.
- `Expose` is the Component → App Maker information channel; Anatomy may read only capabilities explicitly exposed by parts.
- `Template` defines render output; Anatomy does not create, move, or materialize template structure.
- `asHook` may provide behavioral protocol capabilities required by roles; Anatomy requirements only diagnose whether those capabilities exist.
- `Lifecycle` defines availability boundaries for family/claim, runtime query, order query, and cleanup.
