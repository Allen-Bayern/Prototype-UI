id: P-BRUTALIST-SCROLL-AREA-THUMB
type: prototype
title: Brutalist Scroll Area Thumb projects foreground feedback
status: draft
since: 0.2.0-rc.7
summary: Brutalist Thumb inherits host-projected geometry and the family Move hit-subregion role while adding a square foreground fill.
statement:
  zh-CN: Brutalist Thumb 是 composed Scrollbar 内的直角前景反馈面。它继承 Base Thumb 的无独立语义边界与 Move Gesture 命中子区域角色；尺寸、位移与 drag mapping 由家族级 host session 持续管理。
  en: Brutalist Thumb is a square foreground feedback surface inside a composed Scrollbar. It inherits the Base Thumb no-independent-semantics boundary and Move Gesture hit-subregion role; size, offset, and drag mapping remain continuously managed by the family-level host session.
criteria:
  - id: P-BRUTALIST-SCROLL-AREA-THUMB-BASE-INHERITANCE
    text:
      zh-CN: Thumb 必须通过 `asScrollAreaThumb()` 继承 Base anatomy identity、无独立语义边界与 host-managed Move 命中子区域角色。
      en: Thumb must inherit Base anatomy identity, the no-independent-semantics boundary, and the host-managed Move hit-subregion role through `asScrollAreaThumb()`.
  - id: P-BRUTALIST-SCROLL-AREA-THUMB-SURFACE
    text:
      zh-CN: Thumb 必须投射 `relative h-full w-full rounded-none bg-foreground` 作为交叉轴填充，并允许 host session 通过专用 CSS variables/transform 覆盖主轴 geometry；该样式本身不取得 pointer interaction ownership，Move binding 仍属于 host session。
      en: Thumb must project `relative h-full w-full rounded-none bg-foreground` for cross-axis fill and allow the host session to override primary-axis geometry through dedicated CSS variables and transform; the style itself acquires no pointer-interaction ownership, which remains in the host-session Move binding.
sources:
  - path: packages/prototypes/brutalist/src/scroll-area/thumb.proto.ts
dependsOn:
  contracts: [C-AS-HOOK-0001, C-RULE-INTENT-FEEDBACK-STYLE-0001, C-SCROLL-COMPOSED-CONTROL-0001]
inherits:
  prototypes:
    - id: P-BASE-SCROLL-AREA-THUMB
verifies: { tests: [T-BRUTALIST-SCROLL-AREA-0001] }
revisions:
  - version: 0.2.0-rc.7
    change: revised
    summary: Inherited the host-managed Move hit-subregion role without adding independent Thumb semantics.
  - version: 0.2.0-rc.7
    change: revised
    summary: Replaced the full-track flex placeholder with host-projected passive Thumb geometry.
  - version: 0.2.0-rc.7
    change: introduced
    summary: Cataloged the square foreground Thumb feedback surface and explicit no-drag guarantee.
tags: [prototype, brutalist, scroll-area, thumb, feedback, move, visual]
