id: P-SHADCN-TABS
type: prototype
title: Shadcn Tabs Root inherits Base Tabs and adds the current layout surface
status: draft
since: 0.2.0
summary: Shadcn Tabs Root is an unofficial projection that inherits Base Tabs selection and family semantics and adds the currently implemented root layout tokens.
statement:
  zh-CN: >-
    Shadcn Tabs Root 是 `P-BASE-TABS` 之上的 direct prototype，保留 Base Tabs 的 single-selection value、orientation、activation mode、context、anatomy 与 `valueChange` signal，并增加当前 root layout surface。比较基线固定到 shadcn-ui/ui revision `f31ed81983653919dd4fe77aee4b4859f610f1dc` 的 v4 new-york Tabs；`asChild` 明确不提供，其他差异集中记录为 compatibility differences。


  en: >-
    Shadcn Tabs Root is a direct prototype over `P-BASE-TABS`. It retains Base Tabs single-selection value, orientation, activation mode, context, anatomy, and the `valueChange` signal while adding the current root layout surface. Its comparison baseline is the v4 new-york Tabs at shadcn-ui/ui revision `f31ed81983653919dd4fe77aee4b4859f610f1dc`; `asChild` is explicitly unavailable and other differences are centralized as compatibility differences.


criteria:
  - id: P-SHADCN-TABS-UPSTREAM-IDENTITY
    text:
      zh-CN: Root 必须标记为 Proto UI 维护的非官方 shadcn/ui 衍生实现并固定 upstream revision/path；本实体不主张上游所有权或认证。
      en: Root must identify itself as an unofficial Proto UI-maintained shadcn/ui-derived implementation and pin its upstream revision and path; this entity claims neither upstream ownership nor endorsement.
  - id: P-SHADCN-TABS-BASE-INHERITANCE
    text:
      zh-CN: '`shadcn-tabs-root` 必须通过 `inherits.prototypes` 指向 `P-BASE-TABS` 并调用 `asTabsRoot()`；任何 setup-only 负向补丁必须明确声明被改写的 Base guarantee 与替代语义。'
      en: '`shadcn-tabs-root` must point to `P-BASE-TABS` through `inherits.prototypes` and call `asTabsRoot()`; any setup-only negative patch must identify the rewritten Base guarantee and replacement semantics.'
    references:
      prototypes:
        - id: P-BASE-TABS
          anchors:
            [
              P-BASE-TABS-AUTHORING-ENTRIES,
              P-BASE-TABS-ROOT-SEMANTIC-OWNER,
              P-BASE-TABS-ANATOMY-FAMILY,
            ]
    dependsOn:
      contracts: [C-AS-HOOK-0001, C-CORE-SYNTAX-0007]
  - id: P-SHADCN-TABS-CURRENT-BASE-DEVIATIONS
    text:
      zh-CN: 当前实现不反转 inherited value、selection、orientation、activation、context、anatomy、a11y 或 signal guarantee；新增 style tokens 仅构成视觉增量。
      en: The current implementation reverses no inherited value, selection, orientation, activation, context, anatomy, accessibility, or signal guarantee; added style tokens are visual deltas only.
  - id: P-SHADCN-TABS-DIRECT-ENTRY
    text:
      zh-CN: 当前 Root protocol 以 `shadcn-tabs-root` direct prototype 公开，本层不额外提供 authored asHook。
      en: The current Root protocol is exposed as `shadcn-tabs-root`; this layer provides no additional authored asHook.
  - id: P-SHADCN-TABS-CURRENT-VISUAL-SURFACE
    text:
      zh-CN: Root 必须提供当前已实现的 flex column、gap 与 foreground tokens；该 surface 不表示 orientation-aware upstream layout 已对齐。
      en: Root must provide the currently implemented flex-column, gap, and foreground tokens; this surface does not imply parity with upstream orientation-aware layout.
    dependsOn:
      contracts: [C-RULE-INTENT-FEEDBACK-STYLE-0001]
  - id: P-SHADCN-TABS-COMPATIBILITY-SUBSET
    text:
      zh-CN: 当前实体只声明已测试的 inherited family behavior 与 root surface；固定 baseline 的所有差异必须集中在唯一 compatibility open question。
      en: This entity claims only tested inherited family behavior and root surface; every difference from the pinned baseline must be centralized in one compatibility open question.
  - id: P-SHADCN-TABS-AS-CHILD-OMISSION
    text:
      zh-CN: upstream Root props 中的 `asChild` 依 `D-AS-CHILD-OMISSION-0001` 有意不进入 API。
      en: The `asChild` member of upstream Root props is intentionally omitted under `D-AS-CHILD-OMISSION-0001`.
    dependsOn:
      decisions: [D-AS-CHILD-OMISSION-0001]
openQuestions:
  - id: P-SHADCN-TABS-Q-UPSTREAM-DIFFERENCES
    question:
      zh-CN: Shadcn Tabs Root 相对固定 upstream baseline 的全部差异应如何分类与收敛？
      en: How should every Shadcn Tabs Root difference from the pinned upstream baseline be classified and converged?
    context:
      zh-CN: >-
        intentional deviation 是不提供 `asChild`。待实现或待评审项包括 upstream `onValueChange` 相对 `valueChange` signal、className/native prop forwarding、data-slot/data-orientation，以及 horizontal 才采用 flex-column 的方向感知布局；当前 Root 始终为 flex-column、gap-3 并额外设置 foreground。实现并测试前不得扩大 passing guarantee。


      en: >-
        The intentional deviation is omission of `asChild`. Implementation or review gaps include upstream `onValueChange` versus the `valueChange` signal, className and native prop forwarding, data-slot and data-orientation, and orientation-aware layout that uses flex-column only for horizontal tabs; the current Root is always flex-column with gap-3 and an added foreground token. The passing guarantee must not expand before implementation and tests.


    blocks: [shadcn tabs root parity roadmap]
sources:
  - path: internal/records/2026-07-18-external-prototype-library-governance.zh-CN.md
  - path: packages/prototypes/shadcn/src/tabs/root.proto.ts
  - path: packages/prototypes/shadcn/src/tabs/types.ts
  - path: packages/prototypes/shadcn/test/tabs.test.ts
  - path: https://github.com/shadcn-ui/ui/blob/f31ed81983653919dd4fe77aee4b4859f610f1dc/apps/v4/registry/new-york-v4/ui/tabs.tsx
    label: shadcn/ui v4 new-york Tabs pinned baseline
dependsOn:
  contracts:
    [C-AS-HOOK-0001, C-CORE-SYNTAX-0003, C-CORE-SYNTAX-0007, C-RULE-INTENT-FEEDBACK-STYLE-0001]
  decisions: [D-PROTOTYPE-ENTITY-NAMING-0001, D-AS-CHILD-OMISSION-0001]
inherits:
  prototypes:
    - id: P-BASE-TABS
      note: Default inherited protocol; explicit setup-time negative-patch criteria may override individual guarantees.
verifies: { tests: [T-SHADCN-TABS-0001] }
revisions:
  - version: 0.2.0
    change: introduced
    summary: Introduced Shadcn Tabs Root delta cataloging with Base inheritance, current layout surface, intentional asChild omission, and centralized upstream differences.
tags: [prototype, shadcn, tabs, root, upstream-derived, delta, compatibility, visual]
