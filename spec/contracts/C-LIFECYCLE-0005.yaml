id: C-LIFECYCLE-0005
type: contract
title: Prototype lifecycle API surface uses setup-time callback registration
status: draft
since: 0.1.0
summary: Prototype-visible lifecycle syntax registers instance and repeatable mount callbacks during setup.
statement:
  zh-CN: >-
    Proto UI 的 prototype-visible lifecycle API 必须通过 setup 阶段的 `def.lifecycle` surface 注册 runtime callback。portable entry 为 `onCreated`、`onMounted`、`onUpdated`、`onUnmounted` 与 terminal `onBeforeDispose`；registration 不立即调用 callback，也不提供 portable revoke handle。mounted/unmounted callback 按 mount epoch 重复，onBeforeDispose 在 terminal disposal 前执行一次。


  en: >-
    Proto UI's prototype-visible lifecycle API must register runtime callbacks through the setup-time `def.lifecycle` surface. Portable entries are `onCreated`, `onMounted`, `onUpdated`, `onUnmounted`, and terminal `onBeforeDispose`. Registration does not invoke callbacks immediately and provides no portable revoke handle. Mounted/unmounted callbacks repeat per mount epoch, while onBeforeDispose runs once before terminal disposal.


criteria:
  - id: C-LIFECYCLE-0005-A
    text:
      zh-CN: lifecycle setup API 必须由 `def.lifecycle` 承载，而不是由 `run handle`、renderer、adapter option 或 host-specific hook 承载。
      en: The lifecycle setup API must be carried by `def.lifecycle`, not by the `run handle`, renderer, adapter options, or host-specific hooks.
  - id: C-LIFECYCLE-0005-B
    text:
      zh-CN: portable lifecycle registration entries 必须固定为 `onCreated`、`onMounted`、`onUpdated`、`onUnmounted` 与 `onBeforeDispose`。
      en: Portable lifecycle registration entries must be fixed as `onCreated`, `onMounted`, `onUpdated`, `onUnmounted`, and `onBeforeDispose`.
  - id: C-LIFECYCLE-0005-C
    text:
      zh-CN: '`def.lifecycle` 的全部 registration entry 必须是 setup-only；setup 结束后调用必须失败。'
      en: All `def.lifecycle` registration entries must be setup-only; calls after setup exits must fail.
  - id: C-LIFECYCLE-0005-D
    text:
      zh-CN: lifecycle registration API 不得在注册时立即执行 callback；callback 只能由 runtime 生命周期流程调用。
      en: Lifecycle registration APIs must not invoke callbacks during registration; callbacks may only be invoked by runtime lifecycle flow.
  - id: C-LIFECYCLE-0005-E
    text:
      zh-CN: lifecycle callback 执行时必须接收绑定当前 callback invocation 的 `run handle`，并遵守 `C-CORE-SYNTAX-0002`。
      en: Lifecycle callbacks must receive a `run handle` bound to the current callback invocation and must follow `C-CORE-SYNTAX-0002`.
  - id: C-LIFECYCLE-0005-F
    text:
      zh-CN: lifecycle registration API 的返回值不得被解释为 portable unsubscribe、revoke 或 lifecycle handle 能力。
      en: Return values from lifecycle registration APIs must not be interpreted as portable unsubscribe, revoke, or lifecycle-handle capabilities.
  - id: C-LIFECYCLE-0005-G
    text:
      zh-CN: CP checkpoint、host-specific lifecycle hook、adapter diagnostics hook 与 runtime internal lifecycle marker 不得作为 prototype-visible lifecycle API 暴露。
      en: CP checkpoints, host-specific lifecycle hooks, adapter diagnostics hooks, and runtime-internal lifecycle markers must not be exposed as prototype-visible lifecycle APIs.
sources:
  - path: internal/contracts/lifecycle/component-lifecycle.v0.md
    sections:
      - Lifecycle Callbacks
  - path: packages/core/src/handles.ts
    sections:
      - DefHandle.lifecycle
dependsOn:
  contracts:
    - C-LIFECYCLE-0001
    - C-CORE-SYNTAX-0001
    - C-CORE-SYNTAX-0002
revisions:
  - version: 0.1.0
    change: introduced
    summary: Split prototype lifecycle API surface syntax from lifecycle callback ordering.
  - version: 0.1.0
    change: breaking
    summary: Added terminal onBeforeDispose and made mounted/unmounted repeat per mount epoch.
tags:
  - lifecycle
  - syntax
  - setup
  - run-handle
