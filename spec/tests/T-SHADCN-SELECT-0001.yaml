id: T-SHADCN-SELECT-0001
type: test
title: Shadcn Select Root delta protocol contract tests
status: draft
since: 0.2.0-rc.0
summary: Covers Root identity, Base select-only inheritance, current five-part scope, and bounded upstream compatibility.
cases:
  - id: T-SHADCN-SELECT-0001-CASE-ROOT
    title: Root inherits Base select-only ownership and keeps family scope bounded
    covers:
      [
        P-SHADCN-SELECT-UPSTREAM-IDENTITY,
        P-SHADCN-SELECT-BASE-INHERITANCE,
        P-SHADCN-SELECT-CURRENT-BASE-DEVIATIONS,
        P-SHADCN-SELECT-DIRECT-ENTRY,
        P-SHADCN-SELECT-COMPATIBILITY-SUBSET,
      ]
    expectation: shadcn-select-root-current-five-part-family
implementations:
  - id: prototypes-shadcn-select-root-contract
    kind: module-test
    status: passing
    path: packages/prototypes/shadcn/test/select.test.ts
    required: true
    consumesCases: [T-SHADCN-SELECT-0001-CASE-ROOT]
    exercises: [P-SHADCN-SELECT, P-BASE-SELECT]
    notes:
      [
        Tests cover named identity,
        value/open ownership,
        text derivation,
        item commit,
        and five-part coordination; omitted upstream parts remain centralized.,
      ]
verifies:
  prototypes:
    - id: P-SHADCN-SELECT
      anchors:
        [
          P-SHADCN-SELECT-UPSTREAM-IDENTITY,
          P-SHADCN-SELECT-BASE-INHERITANCE,
          P-SHADCN-SELECT-CURRENT-BASE-DEVIATIONS,
          P-SHADCN-SELECT-DIRECT-ENTRY,
          P-SHADCN-SELECT-COMPATIBILITY-SUBSET,
        ]
exercises: { prototypes: [P-SHADCN-SELECT, P-BASE-SELECT] }
revisions:
  [{ version: 0.2.0-rc.0, change: introduced, summary: Added Shadcn Select Root delta coverage. }]
tags: [prototype, shadcn, select, root, delta, compatibility, test]
