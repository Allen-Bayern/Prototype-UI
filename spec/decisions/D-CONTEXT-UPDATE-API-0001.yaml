id: D-CONTEXT-UPDATE-API-0001
type: decision
title: Context keeps separate update and tryUpdate APIs in v0
status: draft
since: 0.1.0
summary: Context v0 keeps `update(key, next)` and `tryUpdate(key, next)` as separate runtime APIs so authors explicitly distinguish required and optional context availability.
notes: ./D-CONTEXT-UPDATE-API-0001.md
explains:
  contracts:
    - C-CONTEXT-0008
revisions:
  - version: 0.1.0
    change: introduced
    summary: Recorded the v0 decision to preserve required/optional update API distinction.
tags:
  - context
  - update
  - api
